{
    "scripts": {
        "install-front": "cd front && npm install",
        "heroku-postbuild": "cd front && npm run install-front && npm run build",
        "start": "cd back && npm install && node index.js"
    }
}
